services:
    urjalink-backend:
        build:
            context: .
            dockerfile: Dockerfile.backend
        container_name: urjalink-backend
        restart: unless-stopped
        ports:
            - "8080:8080"
        env_file:
            - .env